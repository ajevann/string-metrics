function run( calcualtions ) {
    let {
        stringSimilarity,
        fastestLevenshtein, 
        cosineSimilarity
    } = calcualtions;

    stringSimilarity * .4
    fastestLevenshtein * .3
    cosineSimilarity * .3

    return 
}

module.exports = run;